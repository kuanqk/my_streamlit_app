# 📈 Financial Market Dashboard

Streamlit приложение для визуализации финансовых графиков с реальными данными.

## 🚀 Быстрый старт

### Установка зависимостей

```bash
pip install -r requirements.txt
```

### Запуск приложения

**Вариант 1: С примерными данными (демо)**
```bash
streamlit run finance_charts_app.py
```

**Вариант 2: С реальными данными из Yahoo Finance**
```bash
streamlit run finance_charts_real_data.py
```

## 📊 Возможности

- ✅ Визуализация 3 финансовых инструментов (S&P 500, Bitcoin, Gold)
- ✅ Реальные данные через Yahoo Finance API
- ✅ Адаптивный дизайн
- ✅ Цветовая индикация роста/падения
- ✅ Кэширование данных (обновление каждые 5 минут)
- ✅ Выбор периода отображения (1д, 5д, 7д, 1м, 3м)
- ✅ Авто-обновление (опционально)
- ✅ Hover для просмотра точных значений

## 📁 Структура файлов

```
.
├── finance_charts_app.py           # Демо версия с примерными данными
├── finance_charts_real_data.py     # Версия с реальными данными
├── requirements.txt                # Зависимости
└── README.md                       # Документация
```

## 🔧 Настройка

### Изменение тикеров

В файле `finance_charts_real_data.py` найдите раздел `tickers_config`:

```python
tickers_config = {
    "SP500": {"symbol": "^GSPC", "name": "SP500", "currency": "USD"},
    "BTC": {"symbol": "BTC-USD", "name": "BTC", "currency": "USD"},
    "GOLD": {"symbol": "GC=F", "name": "ЗОЛОТО", "currency": "USD"}
}
```

Замените символы на нужные вам тикеры из Yahoo Finance.

### Популярные тикеры:

**Индексы:**
- `^GSPC` - S&P 500
- `^DJI` - Dow Jones
- `^IXIC` - NASDAQ
- `^RUT` - Russell 2000

**Криптовалюты:**
- `BTC-USD` - Bitcoin
- `ETH-USD` - Ethereum
- `BNB-USD` - Binance Coin

**Товары:**
- `GC=F` - Gold Futures
- `CL=F` - Crude Oil
- `SI=F` - Silver

**Акции:**
- `AAPL` - Apple
- `TSLA` - Tesla
- `MSFT` - Microsoft

## 🎨 Кастомизация

### Изменение цветовой схемы

В функции `create_financial_chart()`:

```python
# Цвет для падения/роста
color = '#EF5350' if change_pct < 0 else '#26A69A'

# Цвет фона графика
plot_bgcolor='#f5f5f5'
paper_bgcolor='white'
```

### Изменение высоты графиков

```python
fig.update_layout(
    height=250,  # Измените это значение
    ...
)
```

## 📦 Зависимости

- `streamlit` - фреймворк для веб-приложений
- `plotly` - библиотека для интерактивных графиков
- `pandas` - обработка данных
- `numpy` - математические операции
- `yfinance` - API для финансовых данных

## 🔍 Пример использования с другими API

### Alpha Vantage

```python
import requests

def get_alphavantage_data(symbol, apikey):
    url = f'https://www.alphavantage.co/query?function=TIME_SERIES_INTRADAY&symbol={symbol}&interval=5min&apikey={apikey}'
    response = requests.get(url)
    data = response.json()
    # Обработка данных...
    return df
```

### CoinGecko (для крипты)

```python
import requests

def get_coingecko_data(coin_id):
    url = f'https://api.coingecko.com/api/v3/coins/{coin_id}/market_chart?vs_currency=usd&days=7'
    response = requests.get(url)
    data = response.json()
    # Обработка данных...
    return df
```

## 🐛 Решение проблем

### Ошибка "Данные недоступны"

1. Проверьте подключение к интернету
2. Убедитесь, что тикер существует на Yahoo Finance
3. Попробуйте изменить период/интервал данных
4. Очистите кэш: нажмите кнопку "Обновить данные"

### Медленная загрузка

1. Уменьшите период отображения
2. Увеличьте интервал данных
3. Проверьте кэширование (`@st.cache_data`)

## 📝 Лицензия

MIT License

## 👨‍💻 Автор

Разработано для Өрлеу AI Laboratory

## 🤝 Вклад

Приветствуются Pull Requests и Issue!

## 📞 Поддержка

Если у вас возникли вопросы, создайте Issue в репозитории.
